export interface ViewPort {
  width: number;
  height: number;
}

export interface Scroll {
  ease: number;
  hard: number;
  height: number;
  limit: number;
  normalized: number;
  running: boolean;
  soft: number;
}
